{
  "name": "betlog-baccarat-worker",
  "version": "1.0.0",
  "private": true,
  "description": "BETLOG Live Baccarat — Cloudflare Worker",
  "scripts": {
    "dev":        "wrangler dev",
    "deploy":     "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "type-check": "tsc --noEmit",
    "lint":       "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "~5.8.2",
    "wrangler": "^3.80.0"
  }
}
